vulnerabilities:
  - id: CVE-2016-1000027
    purls:
      - pkg:maven/org.springframework/spring-web@5.3.30
    paths:
      - /openmrs/distribution/openmrs_core/openmrs.war/WEB-INF/lib/spring-web-5.3.30.jar
      - openmrs/distribution/openmrs_core/openmrs.war/WEB-INF/lib/spring-web-5.3.30.jar
    expired_at: 2027-02-13
    statement: |
      Reviewed for GitHub code scanning alert 206. This package is inherited from the
      pinned OpenMRS core 2.8.8 backend image used by the O3 RefApp 3.7.1 baseline.
      LiberiaEMR does not configure or ship Spring HTTP invoker remoting endpoints
      such as HttpInvokerServiceExporter, and the bundled OMODs were checked for that
      pattern. Revisit during the next OpenMRS core/refapp rebase or before expiry.
