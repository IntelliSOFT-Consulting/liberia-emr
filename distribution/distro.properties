# distro.properties — LiberiaEMR distribution
#
# THIS FILE PINS EXACT, TESTED VERSIONS. Never use `latest`, a dynamic version, a range,
# or `-SNAPSHOT` outside development. Ranges belong in each package's content.properties.
# See IMPLEMENTATION.md §6.
#
# RECONCILED 2026-07-28 against a resolved openmrs-distro-referenceapplication
# **3.7.1** manifest (~/openmrs/liberiaemr/openmrs-distro.properties). Every version below
# that also appears in the RefApp is copied verbatim from it — do not hand-edit those
# individually; re-run the reconciliation against a newer RefApp instead.
#
# Entries marked "LiberiaEMR addition" are NOT part of the RefApp tested set and carry
# their own upgrade risk. Entries marked "VERIFY" still need confirmation before release.
#
# The 3.6 -> 3.7.1 rebase and the reasoning behind it are recorded in
# docs/adr/0006-pin-o3-refapp-3.7.1.md, which supersedes ADR 0002.

name=LiberiaEMR
version=1.0.0-SNAPSHOT

# ---------------------------------------------------------------------------
# Platform
# ---------------------------------------------------------------------------
war.openmrs=2.8.8

# ---------------------------------------------------------------------------
# Concept dictionary (OCL)
# ---------------------------------------------------------------------------
# The collections scripts/build/fetch-ciel.sh pulls from the MOH's OCL org, and the released
# collection version to pin them to. The dictionary decides what every concept UUID in
# content resolves to, so it belongs under the same pinning rule as the WAR and the OMODs:
# two builds of one tag must get the same concepts.
#
# ocl.collection.version is INTENTIONALLY EMPTY until the MOH publishes a released version.
# Empty means fetch-ciel.sh exports HEAD, which is reproducible only by accident — the build
# says so out loud. Set it to the released version id before cutting a release.
ocl.org=LIB
ocl.collections=mch
ocl.collection.version=1.0.9

# ---------------------------------------------------------------------------
# Backend modules (OMOD) — O3 RefApp 3.7.1 baseline
# ---------------------------------------------------------------------------
omod.addresshierarchy=2.21.0
omod.appointments=2.1.0
omod.appointments.groupId=org.bahmni.module
omod.attachments=4.0.0
omod.authentication=2.3.0
omod.bedmanagement=7.2.0
omod.billing=2.3.0
omod.calculation=2.0.0
omod.cohort=3.7.3
omod.emrapi=3.4.0
omod.event=4.0.0
omod.event.groupId=org.openmrs
omod.fhir2=4.2.0
omod.htmlwidgets=2.0.1
omod.idgen=5.0.4
omod.initializer=2.12.0
omod.legacyui=2.1.0
omod.metadatamapping=2.0.0
omod.o3forms=2.3.0
omod.openconceptlab=3.1.0
omod.ordertemplates=2.2.0
omod.patientdocuments=1.1.0
omod.patientflags=3.0.10
omod.queue=3.0.0
omod.reporting=2.1.0
omod.reportingrest=2.0.0
omod.serialization.xstream=0.3.0
omod.serialization.xstream.type=omod
omod.stockmanagement=3.0.0
omod.tasks=2.1.0
omod.teleconsultation=2.0.0
omod.teleconsultation.groupId=org.bahmni.module
omod.webservices.rest=3.5.0

# VERIFY — RefApp 3.7.1 itself pins a SNAPSHOT here, which violates the no-SNAPSHOT rule
# at the top of this file. Replace with the released 3.1.0 (or later) before go-live.
omod.spa=3.1.0-SNAPSHOT

# --- LiberiaEMR additions (not in the RefApp tested set) --------------------
# Required by content-packages/content-common/content.properties (>=5.4.0). RefApp 3.7.1
# no longer ships htmlformentry (O3 uses o3forms + esm-form-engine). Keep only while a
# content package still needs it; it depends on omod.htmlwidgets, pinned above.
omod.htmlformentry=5.4.0

# HIS-Lite. billing and stockmanagement come from the RefApp block above.
#
# omod.dispensing WAS pinned at 1.2.0 and is now DROPPED — the VERIFY note that used to sit
# here was right. It is an O2 module: on a real startup it refuses to load with
#   requires the following module(s): htmlformentryui 1.0, appframework 2.0,
#   providermanagement, appui 1.0
# and those O2 UI modules have no place in an O3 distribution. Dispensing in O3 is
# esm-dispensing-app (pinned below) talking FHIR MedicationDispense through fhir2.
# content-liberia-pharmacy's requirement was relaxed to match.
# omod.dispensing=1.2.0
omod.labonfhir=1.2.0

# --- Sync / integration (facility -> central) ---
# These are NOT OMODs. openmrs-eip is a toolbox (openmrs-watcher, the OpenMRS Camel
# component); the deployable sender and receiver applications are
# mekomsolutions/openmrs-dbsync, built on top of it and run as their own containers.
# ADR 0008 records the selection; this closes risk E4 (sync-eip.md §10 item 3).
#
# sync.dbsync is the source tag distribution/sync/Dockerfile builds; sync.eip is the
# openmrs-eip version that dbsync tag declares (its pom's eipVersion), recorded here so
# the pair is explicit. Bump them together, never independently.
sync.dbsync=4.0.0
sync.eip=4.2.0

# ---------------------------------------------------------------------------
# Frontend (O3 app shell + ESMs) — RefApp 3.7.1
# ---------------------------------------------------------------------------
spa.core=10.0.0

spa.frontendModules.@openmrs/esm-primary-navigation-app=10.0.0
# spa.frontendModules.@openmrs/esm-login-app=10.0.0
spa.frontendModules.@openmrs/esm-home-app=11.1.0
spa.frontendModules.@openmrs/esm-help-menu-app=10.0.0
spa.frontendModules.@openmrs/esm-user-onboarding-app=4.1.0
spa.frontendModules.@openmrs/esm-system-admin-app=4.4.0

# --- Registration / search / lists ---
spa.frontendModules.@openmrs/esm-patient-registration-app=11.1.0
spa.frontendModules.@openmrs/esm-patient-search-app=11.1.0
spa.frontendModules.@openmrs/esm-patient-list-management-app=11.1.0
spa.frontendModules.@openmrs/esm-patient-lists-app=12.3.4
spa.frontendModules.@openmrs/esm-active-visits-app=11.1.0

# --- Patient chart ---
spa.frontendModules.@openmrs/esm-patient-chart-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-banner-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-allergies-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-attachments-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-conditions-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-flags-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-forms-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-immunizations-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-label-printing-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-medications-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-notes-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-orders-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-procedures-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-programs-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-task-list-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-tests-app=12.3.4
spa.frontendModules.@openmrs/esm-patient-vitals-app=12.3.4
spa.frontendModules.@openmrs/esm-generic-patient-widgets-app=12.3.4

# --- Forms ---
spa.frontendModules.@openmrs/esm-form-engine-app=12.3.4
spa.frontendModules.@openmrs/esm-form-builder-app=3.5.1
spa.frontendModules.@openmrs/esm-fast-data-entry-app=1.5.0

# --- Service delivery ---
# esm-appointments-app replaces the (non-existent) esm-patient-appointments-app that this
# file previously pinned; it covers both the clinic list and the chart-level workspace.
spa.frontendModules.@openmrs/esm-appointments-app=11.1.0
spa.frontendModules.@openmrs/esm-service-queues-app=11.1.0
spa.frontendModules.@openmrs/esm-ward-app=11.1.0
spa.frontendModules.@openmrs/esm-bed-management-app=11.1.0

# --- HIS-Lite: dispensing, lab, billing, stock ---
spa.frontendModules.@openmrs/esm-dispensing-app=1.11.1
spa.frontendModules.@openmrs/esm-laboratory-app=1.5.1
spa.frontendModules.@openmrs/esm-billing-app=1.3.1
spa.frontendModules.@openmrs/esm-stock-management-app=3.2.1

# --- Terminology / reporting / admin tooling ---
spa.frontendModules.@openmrs/esm-openconceptlab-app=4.4.0
spa.frontendModules.@openmrs/esm-cohort-builder-app=4.0.6
spa.frontendModules.@openmrs/esm-reports-app=4.4.0

# --- Implementer / developer tooling ---
# Part of the RefApp tested set. Both are gated by privileges at runtime; drop them here
# if the MOH decides they must not ship to facilities at all.
spa.frontendModules.@openmrs/esm-implementer-tools-app=10.0.0
spa.frontendModules.@openmrs/esm-devtools-app=10.0.0

# --- Custom Build (IMPLEMENTATION.md §3) ---
# The e-partograph is greenfield: a normal, separately versioned O3 frontend module pinned
# here like any other. It is NOT bundled inside a content package.
#
# Pinned to the exact build verified against spa.core=10.0.0 above. The module's tooling
# (@openmrs/esm-framework, openmrs) must stay on 10.0.0: 10.0.1-pre and newer inject a
# Module Federation runtime guard that the 10.0.0 app shell cannot satisfy, and the module
# refuses to start. Re-pin deliberately after each publish — never `next` (§6).
spa.frontendModules.@liberiaemr/esm-liberia-epartograph-app=next

# Generic obs-by-encounter widget (TB Screening first, then Partograph/ANC summaries).
spa.frontendModules.@liberiaemr/esm-liberia-patient-chart-extension=next

# Custom login application replacing the core esm-login-app
spa.frontendModules.@liberiaemr/esm-liberia-login-app=next

# ---------------------------------------------------------------------------
# Content packages
# ---------------------------------------------------------------------------
# The RefApp content package supplies the minimum O3 metadata baseline. It is NOT a
# production configuration on its own — our layers below add facility locations,
# identifiers, formularies and programme metadata.
content.referenceapplication=1.4.0
content.referenceapplication.groupId=org.openmrs.content

# content.liberiaemr-common=1.0.0-SNAPSHOT
# content.liberiaemr-common.groupId=lr.gov.moh.liberiaemr.content
# content.liberiaemr-national=1.0.0-SNAPSHOT
# content.liberiaemr-national.groupId=lr.gov.moh.liberiaemr.content
# content.liberiaemr-mch=1.0.0-SNAPSHOT
# content.liberiaemr-mch.groupId=lr.gov.moh.liberiaemr.content
# content.liberiaemr-lab=1.0.0-SNAPSHOT
# content.liberiaemr-lab.groupId=lr.gov.moh.liberiaemr.content
# content.liberiaemr-pharmacy=1.0.0-SNAPSHOT
# content.liberiaemr-pharmacy.groupId=lr.gov.moh.liberiaemr.content
# content.liberiaemr-opd-ipd=1.0.0-SNAPSHOT
# content.liberiaemr-opd-ipd.groupId=lr.gov.moh.liberiaemr.content

# Site layer: exactly ONE of these is selected per facility build via
# scripts/build/build-distribution.sh --site <careysburg|barnersville>.
#content.liberiaemr-site-careysburg=1.0.0-SNAPSHOT
#content.liberiaemr-site-careysburg.groupId=lr.gov.moh.liberiaemr.content

# content.liberiaemr-site-barnersville=1.0.0-SNAPSHOT
# content.liberiaemr-site-barnersville.groupId=lr.gov.moh.liberiaemr.content

# Demo content is never part of a production distribution; training stacks add it via the
# demo compose overlay. The RefApp's own demo artefacts are therefore left out on purpose:
#   content.referenceapplication-demo=1.9.2
#   omod.referencedemodata=2.6.1
# content.liberiaemr-demo is DELIBERATELY ABSENT for the same reason.
#
# content-demo IS that 1.9.2 demo content, lifted into the repository rather than resolved
# as the artefact above, so a training stack builds offline. Nothing here pins it: the demo
# image gets it from DEMO_PACKAGE in distribution/backend/Dockerfile, which only
# scripts/build/build-distribution.sh --demo sets. omod.referencedemodata stays out either
# way — the lifted package is metadata, and no demo PATIENTS are generated.

# ---------------------------------------------------------------------------
# In-tree OMOD — NOT pinned here on purpose
# ---------------------------------------------------------------------------
# modules/liberiaemr (password reset, and the SMS/MFA provisions after it) is built from
# the source in THIS commit by a stage of distribution/backend/Dockerfile and copied into
# /openmrs/distribution/openmrs_modules/ beside the OMODs above.
#
# It is deliberately absent from the omod.* list. Every entry there is resolved from
# mavenrepo.openmrs.org by scripts/build/resolve-modules.sh; liberiaemr has never been
# published to that repository, so a pin would name a coordinate that cannot resolve — and
# the resolver's count check would fail the build rather than ship a short module set.
#
# Building from the tag's own source is the stronger guarantee in any case: there is no
# coordinate that can drift out from under the pin. If the module is ever published
# externally, move it into the omod.* list and delete the module stage from the Dockerfile.
